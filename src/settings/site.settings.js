import { HouseIcon, UserGroupIcon } from '@/components/icons'

export const siteSettings = {
  menus: [
    { id: 1, Icon: HouseIcon, to: '/' },
    { id: 2, Icon: UserGroupIcon, to: '/friend' },
  ],
}
